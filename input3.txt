" Конфигурация IoT-устройства

deviceid := 'sensor123';
firmwareversion := '1.0.2';

settings := table([
    samplingrate = 60,
    units = 'Celsius'
]);

network := table([
    ssid = 'MyWiFi',
    password = 'password123'
]);
